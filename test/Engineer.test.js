const Engineer = require('../lib/Engineer');

describe('Engineer', () => {
    describe('Initialization', () => {
        it('should create an object with Employee properties and a GitHub if provided valid arguments', () => {
          const engineer = new Engineer('Leroy');
        });
    });
});